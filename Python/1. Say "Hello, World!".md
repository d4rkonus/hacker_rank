--------

```python
my_hello = "Hello, World!"
print(my_hello)
```

En este código se define una variable llamada **my_hello** que contendrá el texto que se mostrara en el output, y se usa un **print()** para mostrar el texto que se guarda en la variable. 